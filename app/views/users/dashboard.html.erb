<div class="container">
<h1>My current Bikers</h1>
  <% @bikers.each do |biker| %>
    <!-- DISPLAY MY BIKERS HERE -->
    <div class="notification">
      <img src="../../assets/images/avatar-placeholder.png" class="avatar-large" />
      <div class="notification-content">
        <p><small><%= biker.pseudo %></small></p>
        <p><%= biker.address %></p>
      </div>

      <div class="notification-actions">

        <%= link_to 'Edit' do %>
          <i class="fas fa-pencil-alt"></i>
        <% end %>

        <%= link_to 'Delete', method: :delete, data: { confirm: "You will destroy #{biker.pseudo.upcase}, Are you sure about that?" } do %>
          <i class="far fa-trash-alt"></i></a>
        <% end %>

      </div>
    </div>      
  <% end %>

  <%= link_to 'Add a new Biker!', new_biker_path, class: 'btn btn-primary'%>
</div>